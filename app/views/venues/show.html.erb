<div id="smallbanner">
<br>
<br>
<br>
<br>
<br>

  <h1 class="fontsmallbanner"> Here are this venue's information </h1>
  <p class="fontsmallbanner"></p>
<br>
<br>

<br>



<a class="button" <%= link_to 'Back to Home', root_path %> </a>
<br>
<br>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-md-4">
      <%= render 'venues/card', venue: @venue %>
    </div>
     <% if @venue.user == current_user %>
        <%= link_to 'Edit this Venue', edit_venue_path(@venue), class: "button" %>
        <%= link_to 'View bookings', venue_bookings_path(@venue, @booking), class: "button" %>
        <%= link_to 'Delete this Venue', venue_path(@venue), method: :delete, data: { confirm: "Are you sure you want to delete this Venue?" }, class: "button" %>
      <% else %>
        <%= link_to 'Book this Venue', new_venue_booking_path(@venue, @booking), class: "button" %>
      <% end %>
  </div>
</div>



